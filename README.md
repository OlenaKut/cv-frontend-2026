# CV Next.js Scaffold

This is a minimal Next.js project that recreates the attached CV layout.

Quick start:

```bash
cd "~/Desktop/Portfolio OK/cv_admin_2026"
npm install
npm run dev
```

- Open http://localhost:3000
- Add a profile image at `public/profile.jpg` to show the avatar.
